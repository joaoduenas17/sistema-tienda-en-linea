<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Gestión de Productos</title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1>Gestión de Productos</h1>
        <form action="/products/add" method="post">
            <label for="name">Nombre del Producto:</label>
            <input type="text" id="name" name="name" required>
            <label for="price">Precio:</label>
            <input type="number" id="price" name="price" required>
            <label for="description">Descripción:</label>
            <textarea id="description" name="description" required></textarea>
            <button type="submit">Añadir Producto</button>
        </form>
        <hr>
        <h2>Lista de Productos</h2>
        {{#each products}}
        <div>
            <h3>{{this.name}}</h3>
            <p>Precio: ${{this.price}}</p>
            <p>{{this.description}}</p>
            <form action="/products/edit/{{this.id}}" method="post">
                <input type="text" name="name" placeholder="Nuevo nombre" required>
                <input type="number" name="price" placeholder="Nuevo precio" required>
                <textarea name="description" placeholder="Nueva descripción" required></textarea>
                <button type="submit">Editar</button>
            </form>
            <form action="/products/delete/{{this.id}}" method="post">
                <button type="submit">Eliminar</button>
            </form>
        </div>
        <hr>
        {{/each}}
    </div>
</body>
</html>
